// frontend/src/constants/quiz.js
// Re-exports from @zinko/shared — the single source of truth.
// Existing imports in this codebase (import { ROUNDS } from '../constants/quiz') continue to work unchanged.
export { QUIZ_RULES, ROUNDS, ANSWER_SLOTS } from '@zinko/shared';
